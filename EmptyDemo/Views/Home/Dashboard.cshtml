@model DashboardViewModel
    @*navbar-light bg-light*@
    <nav class="navbar navbar-expand-md ">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mr-auto">
            </ul>
            <ul class="navbar-nav">
                <li class="nav-item">
                </li>
                <li class="nav-item">
                    <form method="post" asp-controller="Home" asp-action="Search">
                        <input id="srch" asp-for="@Model.srch" type="text" class="form-control search-menu" placeholder="Search an Item..." style="width:200px;">
                    </form>
                </li>
            </ul>
        </div>
    </nav>

    <p class="display-4">Our Special Menu's</p>
    <hr />

    <div class="row">
        @foreach (var item in Model.Special)
        {
            <partial name="_ItemPartial" model="item" />
        }
    </div>

    <div>

    </div>

    <p class="display-4" style="padding-top:30px">Menu's in Special Offer</p>
    <hr />

    <div class="row">
        @foreach (var item in Model.Discount)
        {
            <partial name="_ItemPartial" model="item" />
        }
    </div>




    @*<div class="input-group-append">
            <span class="input-group-text">
                <i class="fa fa-search" aria-hidden="true"></i>
            </span>
        </div>*@
